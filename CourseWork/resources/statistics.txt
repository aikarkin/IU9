Статистика
    число итераций
    число итераций поиска по образцу
    число ложных срабатываний поиска по образцу
    число итераций исследующего поиска
    
    конечный результат:
        — суммарное расстояние между молекулами (!!)
        — средднее расстояние между молекулами
        — масимальное расстояние мужду молекулами (!!)
        — минимальное расстояние мужду молекулами (!!)
        
        — суммарная величиана пересечения (!!)
        — средняя величина пересечения
        — минимальная величина пересечения (!!)
        — максимальная величина пересечения (!!)
         
        — колличество пустых ячеек (!!)
        — колличество пересечений (!!)
        — колличество упакованных молекул (!!)
